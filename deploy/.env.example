# Kopiere diese Datei auf dem Pi nach .env (im Projekt-Root) und trage deine Werte ein.
# Wichtig: .env wird NICHT eingecheckt (steht in .gitignore) — Token bleibt geheim.
TELEGRAM_TOKEN=dein_token_hier
CHAT_ID=deine_chat_id_hier
